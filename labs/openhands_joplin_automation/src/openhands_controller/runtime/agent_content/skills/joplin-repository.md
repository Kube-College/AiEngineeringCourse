# Joplin repository

The Joplin checkout is a Yarn monorepo. Shared note logic lives in `packages/lib`; desktop UI and integration code live in `packages/app-desktop`. Inspect the relevant source and nearby tests before choosing a change or validation command. Use the checked-out source as the authority for paths and scripts.
