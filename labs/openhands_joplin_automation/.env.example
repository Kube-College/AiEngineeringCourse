# Proposed settings; application support follows during implementation.
AGENT_BACKEND=simulated
STATE_DIR=.data
WORKSPACE_DIR=.workspaces
GITHUB_WRITES_ENABLED=false
GH_REPO=
GH_TOKEN=
LLM_MODEL=
LLM_API_KEY=
# Optional OpenAI-compatible endpoint, subject to chosen SDK configuration.
LLM_BASE_URL=
